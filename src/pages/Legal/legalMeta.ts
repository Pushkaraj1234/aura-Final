// Details that belong to whoever operates this deployment rather than to the
// code. Set CONTACT_EMAIL before launch: both legal pages name it as the
// address for privacy questions, corrections and erasure requests. Left blank,
// they say plainly that no address has been published yet, which is honest;
// inventing one would be worse than saying nothing.
export const OPERATOR_NAME = "AURA Humanitarian Technology Initiative";
export const CONTACT_EMAIL = "";

// Shown at the top of both documents. Change it whenever the wording changes,
// not on every deploy: a date that moves without the text moving tells a
// reader nothing.
export const LAST_UPDATED = "13 September 2026";
